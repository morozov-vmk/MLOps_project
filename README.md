# Cashback Activation Prediction

## Бизнес-цель

**Прогнозирование вероятности активации кешбек-предложений пользователями** для повышения эффективности маркетинговых кампаний и увеличения вовлеченности клиентов.

### Проблема бизнеса
Банк тратит значительные ресурсы на рассылку кешбек-предложений, но только часть пользователей активирует их. Это приводит к:
- Неэффективному использованию маркетингового бюджета
- Раздражению пользователей ненужными предложениями
- Упущенной выгоде от неактивированных кешбеков

### ML-решение
Разработка модели машинного обучения, которая предсказывает вероятность активации кешбек-предложений каждым пользователем, позволяя:
- Персонализировать рассылку предложений
- Увеличить конверсию активаций
- Снизить затраты на маркетинг

## Набор данных

### Источники данных
- **Исторические транзакции** - данные о покупках пользователей
- **Поведенческие метрики** - активность в мобильном приложении
- **Демографические данные** - сегментация пользователей
- **История кешбек-активаций** - предыдущие реакции на предложения

### Структура данных
- **370 признаков** включая:
  - `cat_viewed_cnt_1m` - просмотры категорий за 1 месяц
  - `cat_activated_cnt_3m` - активации за 3 месяца  
  - `transaction_turnover_ratio_6m` - оборот транзакций
  - `cluster_cat_activated_ratio` - активность в кластере
- **Целевая переменная**: `active_essence` (бинарная: 0/1)
- **Объем**: 1M+ записей в тренировочном и тестовом наборах (данные для примера урезаны)

### Предобработка данных
- Очистка от NaN и infinite значений
- Масштабирование числовых признаков
- Автоматическое определение фичей (исключая служебные колонки)

## Целевые метрики

### Бизнес-метрики
- **Увеличение конверсии активаций**
- **Снижение маркетинговых затрат**

### Метрики качества модели
- **AUC-ROC**: ≥ 0.8
- **Precision**: ≥ 0.78 (минимизация ложных срабатываний)
- **Recall**: ≥ 0.72 (охват целевой аудитории)
- **F1-Score**: ≥ 0.75

### Технические метрики
- **Время инференса**: <= 30 мин на выборке из всех пользователей (это оффлайн решение, инференс раз в месяц)
- **Использование памяти**: ≤ 512 GB RAM
- **Время обучения модели**: ≤ 6 часов

## Архитектура решения

### Модель
```python
CashbackMLP(
    input_dim=370,
    hidden_layers=[512, 256, 128, 64, 32],
    dropout_rates=[0.4, 0.3, 0.3, 0.2, 0.1],
    use_batch_norm=True
)
```

### Технологический стек
- **Фреймворк**: PyTorch
- **Обработка данных**: pandas, scikit-learn
- **Тестирование**: pytest, pre-commit
- **CI/CD**: GitHub Actions

## План экспериментов

### Фаза 1: Базовые эксперименты
1. **Бейзлайн модели** - сравнение с CatBoost
2. **Feature engineering** - анализ важности признаков
3. **Гиперпараметры** - поиск оптимальной архитектуры

### Фаза 2: Оптимизация
1. **Регуляризация** - борьба с переобучением
2. **Балансировка классов** - обработка несбалансированных данных
3. **Ансамблирование** - комбинация нескольких моделей

### Фаза 3: Валидация
1. **Backtesting** - проверка на исторических данных
2. **A/B тестирование** - сравнение с текущей системой
3. **Бизнес-валидация** - оценка реального воздействия

## Инструкции ниже для задания 1: ветка task_1_3

## Запуск


```bash
# Клонирование репозитория
git clone https://github.com/morozov-vmk/MLOps_project.git
cd MLOps_project

# Создание виртуального окружения
python -m venv venv
source venv/bin/activate  # Windows: venv\Scripts\activate

# Установка зависимостей
pip install -r requirements.txt
```

### Обучение модели

```bash
# Базовое обучение
python train.py

# С другим конфигом
python train.py --config config/custom_config.yaml
```

### ВАЖНО!
Датасеты лежат по [ссылке](https://drive.google.com/drive/folders/1-NeQRUze3xzTSZndjm6rIWCsgXnxtPGI), их нужно скачать и положить в директорию `datasets`

### Конфигурация

Основные параметры в `config/model_config.yaml`


### Тестирование

```bash
# Запуск всех тестов
python -m pytest tests/ -v

# С покрытием кода
python -m pytest tests/ --cov=src --cov-report=html

# Проверка стиля кода
pre-commit run --all-files
```


### Автоматические проверки
- **Форматирование**: Black
- **Тесты**: pytest
- **CI/CD**: GitHub Actions при каждом коммите
- Настроен автоматический запуск тестов при коммитах
- Показано что при некорректном коммите тесты выдают ошибку

## Инструкции ниже для задания 2: ветка task2

Восстановление версии проекта

```bash
git clone https://github.com/morozov-vmk/MLOps_project.git
cd MLOps_project
git checkout v0.2
dvc pull
dvc repro
```

Проверка переключения версий проекта

```bash
git checkout v0.1 && dvc pull
git checkout v0.2 && dvc pull
```

Физически данные dvc хранятся в локальной папке (из-за общих по группе проблем подключения к удалённому хранилищу). [Содержимое папки](https://drive.google.com/file/d/1MpU2_ktFmbCbxzZlXfZW8Fz-1dkzWwql/view?usp=share_link). Были реализованы 3 этапа: prepare, train, evaluate. Параметры можно менять в файле `model_config.yaml`. План экпериментов подразумевается использование разных датасетов для обучения (с разными наборами признаков) и сравнение качества.

Чтобы посмотреть результаты в MLFlow, нужно выполнить команду `mlflow ui` и открыть `http://127.0.0.1:5000`. Каждый эксперимент создаёт отдельный run с параметрами, метриками и артефактами.


## Как собрать образ и запустить контейнер (инференс)

### 1) Сборка Docker-образа
```bash
docker build -t ml-app:v1 .
```
### 2) Пример запуска (вход — processed_data.npz, который генерируется prepare.py)
```bash
# подготовьте processed .npz локально (если отсутствует):
python scripts/prepare.py --config config/model_config.yaml --output data/processed/processed_data.npz

# затем:
docker run --rm \
  -v "$(pwd)/artifacts:/app/artifacts:ro" \
  -v "$(pwd)/data/processed/processed_data.npz:/app/processed.npz:ro" \
  -v "$(pwd)/out:/app/out" \
  ml-app:v1 --input_path /app/processed.npz --output_path /app/out/preds.csv --model_path /app/artifacts/model_weights.pth
```
### 3) Что делает скрипт predict:
- Загружает конфиг --config (по умолчанию config/model_config.yaml, при отсутствии — работает без него).
- Загружает веса модели (укажите --model_path или положите модель в artifacts/model_weights.pth или artifacts/best_model.pth).
- Принимает --input_path (поддерживается .npz (ключ X_test), .csv, .parquet).
- Делает предсказания (вероятности и бинарные метки с порогом 0.5).
- Сохраняет `out/pred.csv` с колонками input_id (если был), index, probability, prediction.

---